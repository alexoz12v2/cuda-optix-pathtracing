/**
 * @file testdmtpartitions-moretrig.cppm
 * @brief Partition Interface Unit `moretrig` for `testdmtpartitions` with Additional trigonometric utilities partition.
 */

module;
export module testdmtpartitions:moretrig;

namespace dmt {
    /**
     * @brief Example function for future extensions.
     * @param x The input value (in radians).
     * @return An example operation result (currently uses `myCos`).
     */
    export float someFunc(float x);
} // namespace dmt
